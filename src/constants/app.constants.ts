// App's name
export const APP_NAME = "InvoiceHub" as const;

// App side navigation's width
export const APP_SIDE_NAV_WIDTH = "17.5rem" as const;

// App locales
export const APP_LOCALES = "id" as const;

// App currency
export const APP_CURRENCY = "IDR" as const;
